import { TextoMayusculaDirective } from './texto-mayuscula.directive';

describe('TextoMayusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoMayusculaDirective();
    expect(directive).toBeTruthy();
  });
});
