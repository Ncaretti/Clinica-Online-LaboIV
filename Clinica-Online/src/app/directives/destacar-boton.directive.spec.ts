import { DestacarBotonDirective } from './destacar-boton.directive';

describe('DestacarBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarBotonDirective();
    expect(directive).toBeTruthy();
  });
});
