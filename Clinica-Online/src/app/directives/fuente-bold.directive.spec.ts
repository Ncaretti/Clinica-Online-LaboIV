import { FuenteBoldDirective } from './fuente-bold.directive';

describe('FuenteBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new FuenteBoldDirective();
    expect(directive).toBeTruthy();
  });
});
