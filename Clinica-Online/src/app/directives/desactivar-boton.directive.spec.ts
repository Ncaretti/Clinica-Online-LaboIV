import { DesactivarBotonDirective } from './desactivar-boton.directive';

describe('DesactivarBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new DesactivarBotonDirective();
    expect(directive).toBeTruthy();
  });
});
