import { FechaTurnosPipe } from './fecha-turnos.pipe';

describe('FechaTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
