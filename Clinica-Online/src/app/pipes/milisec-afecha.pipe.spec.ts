import { MilisecAFechaPipe } from './milisec-afecha.pipe';

describe('MilisecAFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new MilisecAFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
