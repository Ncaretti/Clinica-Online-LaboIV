import { FechaSolicitarTurnoPipe } from './fecha-solicitar-turno.pipe';

describe('FechaSolicitarTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaSolicitarTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
