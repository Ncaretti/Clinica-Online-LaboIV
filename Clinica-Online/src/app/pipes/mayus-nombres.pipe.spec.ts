import { MayusNombresPipe } from './mayus-nombres.pipe';

describe('MayusNombresPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusNombresPipe();
    expect(pipe).toBeTruthy();
  });
});
